"""
Django settings for mbakop_polls project.

Generated by 'django-admin startproject' using Django 4.1.2.

For more information on this file, see
https://docs.djangoproject.com/en/4.1/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/4.1/ref/settings/
"""
import os

from pathlib import Path



# Build paths inside the project like this: BASE_DIR / 'subdir'.
import mbakop_polls.secret_settings

BASE_DIR = Path(__file__).resolve().parent.parent


# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/4.1/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'dimitriromaric16'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

ALLOWED_HOSTS = []


# Application definition

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'polls.apps.PollsConfig',
    'accounts',
    'polymorphic',
    'bootstrap3',
    'bootstrap4',
    'django_bootstrap5'
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'mbakop_polls.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [BASE_DIR / 'templates']
        ,
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

# Percorso in cui verranno salvati i file caricati
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')

# URL pubblico per accedere ai file caricati
MEDIA_URL = '/media/'

# ...

# Configurazione del backend di archiviazione dei file (utilizziamo il backend predefinito)
DEFAULT_FILE_STORAGE = 'django.core.files.storage.FileSystemStorage'



WSGI_APPLICATION = 'mbakop_polls.wsgi.application'
# Database
# https://docs.djangoproject.com/en/4.1/ref/settis
# settings.py

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': os.path.join(BASE_DIR, 'db.sqlite3'),
        'USER': '',  # Nom d'utilisateur (si applicable)
        'PASSWORD': '',  # Mot de passe (si applicable)
        'HOST': '',  # Hôte de la base de données (si applicable)
        'PORT': '',  # Port de la base de données (si applicable)
    }
}




# Password validation
# https://docs.djangoproject.com/en/4.1/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/4.1/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/4.1/howto/static-files/

STATIC_URL = '/static/'

# Default primary key field type
# https://docs.djangoproject.com/en/4.1/ref/settings/#default-auto-field

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

STATICFILES_DIRS = [os.path.join(BASE_DIR, 'polls/static'),
                    ]
APPEND_SLASH = True

LOGIN_URL = 'login'
LOGOUT_URL = 'logout'
LOGIN_REDIRECT_URL = 'home'
LOCALE_PATHS = [os.path.join(BASE_DIR, 'locale'), ]
AUTH_USER_MODEL = 'accounts.MyPollAppUser'

BOOTSTRAP3 = {
    'horizontal_label_class': 'col-md-4',
    'horizontal_field_class': 'col-md-8',
}
EMAIL_HOST = mbakop_polls.secret_settings.EMAIL_HOST
EMAIL_HOST_USER = mbakop_polls.secret_settings.EMAIL_HOST_USER
EMAIL_HOST_PASSWORD = mbakop_polls.secret_settings.EMAIL_HOST_PASSWORD
EMAIL_PORT = mbakop_polls.secret_settings.EMAIL_PORT
EMAIL_USE_TLS = mbakop_polls.secret_settings.EMAIL_USE_TLS
EMAIL_BACKEND = mbakop_polls.secret_settings.EMAIL_BACKEND
EMAIL_FROM = mbakop_polls.secret_settings.EMAIL_FROM

BASE_URL = mbakop_polls.secret_settings.BASE_URL